@shorttitle(Emuteca: Introduction)
@title(General introduction to Emuteca)

By now, Emuteca is split in two subprojects

@definitionList(
  @itemLabel(Emuteca Core @(cEmuteca@))
    @item(It is the database engine and some sample forms.)
  @itemLabel(Emuteca GUI @(TfmETKGUIMain@)
    @item(It is a full working GUI and Editor with media support.)
)

In the future, may be an SDL playing only GUI will be developed.